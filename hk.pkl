amends "package://github.com/jdx/hk/releases/download/v1.45.0/hk@1.45.0#/Config.pkl"

// TODO: mainでのみ実行するようにする
hooks {
  ["pre-push"] {
    steps {
      ["test"] {
        check = "bundle exec rake test"
      }
      ["check-format"] {
        check = "bundle exec rake check_format"
      }
      ["lint"] {
        check = "bundle exec rake lint"
      }
    }
  }
}
